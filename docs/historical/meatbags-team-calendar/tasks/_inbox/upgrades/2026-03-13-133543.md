# Upgrade Report

- Run: `2026-03-13-133543`
- Target flow version: `2026-03-agent-process-v1`
- Project root: `playground/team-calendar`
- Outcome: `completed_with_followups`

## Current Project State Summary

- `rep.config.json` exists with `task_prefix=TEAMCAL`, `task_sync.mode=local+linear`, and `flow_version=2026-03-agent-process-v1`.
- Managed process artifacts exist, but dry-run upgrade showed additional drift beyond `arch-patterns`.
- This rollout was intentionally narrowed to the new seed rule only, without forcing unrelated process changes.

## Artifact Classification

### `create_missing`
- None.

### `update_managed`
- `tasks/_policies/arch-patterns.md`

### `unchanged`
- `tasks/_policies/project-patterns.md`
- `tasks/_inbox/retro-inbox.md`
- `tasks/_templates/feature.template.md`
- `tasks/_templates/feature-state.template.json`
- `tasks/_templates/task-testing.template.md`
- `tasks/_templates/task-retro.template.md`
- `tasks/_templates/feature-retro.template.md`
- `AGENTS.md`

### `manual_followup`
- File: `rep.config.json`
  Reason: dry-run upgrade would replace the current config with a newer managed shape, which is outside the scope of this rollout.
  Required action: run a dedicated `upgrade project` flow for `playground/team-calendar` when you want the full process contract upgrade.
  Blocking: `false`
  Owner: `agent`
- File: `tasks/_policies/dev-plan.md`
  Reason: dry-run upgrade detected upstream drift, but this rollout intentionally targeted only `arch-patterns`.
  Required action: include `dev-plan.md` in the next full project upgrade.
  Blocking: `false`
  Owner: `agent`
- File: `tasks/_templates/task-state.template.json`
  Reason: dry-run upgrade detected upstream drift in the managed template, but this rollout intentionally excluded template changes.
  Required action: include this template in the next full project upgrade.
  Blocking: `false`
  Owner: `agent`

## Planned Changes

- Replace `tasks/_policies/arch-patterns.md` from the current `project-tracker` seed.
- Do not apply unrelated `rep.config/dev-plan/template` changes under the guise of the `AP-071` rollout.

## Applied Changes

- Replaced `tasks/_policies/arch-patterns.md` from `.agent/skills/project-tracker/references/arch-patterns.seed.md`.
- Created `tasks/_inbox/upgrades/` to store the upgrade audit trail for this project root.

## Pending Changes

- `rep.config.json` still needs a dedicated full upgrade.
- `tasks/_policies/dev-plan.md` still needs a dedicated full upgrade.
- `tasks/_templates/task-state.template.json` still needs a dedicated full upgrade.

## Manual Follow-up Details

### `rep.config.json`

- A full upgrade would update the managed config shape.
- That change was intentionally excluded from this narrow rollout.

### `tasks/_policies/dev-plan.md`

- The full upgrade helper reported drift against the current seed.
- This file was intentionally not changed in the narrow rollout.

### `tasks/_templates/task-state.template.json`

- The full upgrade helper reported drift against the current managed template.
- Template refresh should happen in a dedicated full upgrade, not piggybacked on the `AP-071` rollout.
