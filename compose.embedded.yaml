services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        APP_HOME: ${CONTAINER_APP_HOME:?CONTAINER_APP_HOME is required}
        PORT: ${PORT:?PORT is required}
    restart: unless-stopped
    ports:
      - "${PORT:?PORT is required}:${PORT:?PORT is required}"
    environment:
      APP_HOME: ${CONTAINER_APP_HOME:?CONTAINER_APP_HOME is required}
      PORT: ${PORT:?PORT is required}
      NODE_ENV: production
      APP_BASE_URL: ${APP_BASE_URL:?APP_BASE_URL is required}
      NEXTAUTH_URL: ${NEXTAUTH_URL:?NEXTAUTH_URL is required}
      NEXTAUTH_SECRET: ${NEXTAUTH_SECRET:?NEXTAUTH_SECRET is required}
      TOKEN_ENC_KEY: ${TOKEN_ENC_KEY:?TOKEN_ENC_KEY is required}
      GOOGLE_CLIENT_ID: ${GOOGLE_CLIENT_ID}
      GOOGLE_CLIENT_SECRET: ${GOOGLE_CLIENT_SECRET}
      EMBEDDED_POSTGRES_HOST: ${EMBEDDED_POSTGRES_HOST:?EMBEDDED_POSTGRES_HOST is required}
      EMBEDDED_POSTGRES_PORT: ${EMBEDDED_POSTGRES_PORT:?EMBEDDED_POSTGRES_PORT is required}
      EMBEDDED_POSTGRES_DB: ${EMBEDDED_POSTGRES_DB:?EMBEDDED_POSTGRES_DB is required}
      EMBEDDED_POSTGRES_USER: ${EMBEDDED_POSTGRES_USER:?EMBEDDED_POSTGRES_USER is required}
      EMBEDDED_POSTGRES_PASSWORD: ${EMBEDDED_POSTGRES_PASSWORD:?EMBEDDED_POSTGRES_PASSWORD is required}
      EMBEDDED_POSTGRES_DATA_DIR: ${CONTAINER_EMBEDDED_POSTGRES_DATA_DIR:?CONTAINER_EMBEDDED_POSTGRES_DATA_DIR is required}
      EMBEDDED_POSTGRES_CREATE_USER: ${EMBEDDED_POSTGRES_CREATE_USER:?EMBEDDED_POSTGRES_CREATE_USER is required}
      RATE_LIMIT_WINDOW_MIN: ${RATE_LIMIT_WINDOW_MIN}
      RATE_LIMIT_AVAILABILITY_MAX: ${RATE_LIMIT_AVAILABILITY_MAX}
      RATE_LIMIT_BOOKING_MAX: ${RATE_LIMIT_BOOKING_MAX}
      SMTP_HOST: ${SMTP_HOST}
      SMTP_PORT: ${SMTP_PORT}
      SMTP_SECURE: ${SMTP_SECURE}
      SMTP_USER: ${SMTP_USER}
      SMTP_PASS: ${SMTP_PASS}
      SMTP_FROM_EMAIL: ${SMTP_FROM_EMAIL}
      SMTP_FROM_NAME: ${SMTP_FROM_NAME}
      BOOKING_WEBHOOK_URL: ${BOOKING_WEBHOOK_URL}
      BOOKING_TOKEN: ${BOOKING_TOKEN}
    command: ["sh", "-lc", "npm run db:migrate && npm run start"]
    volumes:
      - team-calendar-embedded-data:${CONTAINER_EMBEDDED_POSTGRES_DATA_DIR:?CONTAINER_EMBEDDED_POSTGRES_DATA_DIR is required}
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "node -e \"fetch('http://127.0.0.1:' + process.env.PORT + '/api/health').then((r) => process.exit(r.ok ? 0 : 1)).catch(() => process.exit(1))\""
        ]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 30s

volumes:
  team-calendar-embedded-data:
    name: team-calendar-embedded-data
